name: CI

on:
  push:
    branches: [main, fusion-v0, fusion-v1-resident-state]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: python -m pip install -e '.[test]'
      - name: Test
        run: pytest -q
      - name: Rebuild frozen receipts
        run: |
          python -m experiments.run_v0 --out /tmp/v0.json
          cmp /tmp/v0.json results/v0.json
          python -m experiments.run_v1 --out /tmp/v1.json
          cmp /tmp/v1.json results/v1.json
